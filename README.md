# Structurizer using Typescript

Prototype is now working.

## To Do

Some real modeling, the application now works and uploaded the model and produces an 
output.puml file locally. This can then be rendered by GraphViz 
